import { Box } from "@mui/material"
import DataGridClients from "../DataGridClients"

export default function ClientsContent() {

    return (
        <Box sx={{ width: '100%' }}>
            <DataGridClients />
        </Box>
    )
}